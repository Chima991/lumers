<span>
    <span class="filmtitle">{{currentFilm.title}}</span>
    <a class="backfilm waves-effect waves-light large btn" href="/"><i class="large material-icons right">arrow_back</i>Вернуться к фильмам</a>
</span>

<div class="filmpage">
    <div class="img">
        <img src="{{currentFilm.img}}" width="200" alt="{{currentFilm.title}}">
    </div>
    <div class="info">
        <div>Год выпуска: {{currentFilm.year}}</div>
        <div>Жанр: {{currentFilm.genre}}</div>
        <div>Описание: {{currentFilm.description}}</div>
    </div>
    <div class="buttonFilm">
        <div>
            <a href="{{currentFilm.id}}/edit?allow=true" type="submit" class="waves-effect waves-light btn"><i class="material-icons right">edit</i>Редактировать</a>
        </div>
        <div>
            <form action="/delete" method="POST"> 
            <input type="hidden" name="id" value="{{currentFilm.id}}"> 
            <button class="waves-effect waves-light btn-delete btn"><i class="material-icons right">delete</i>Удалить</button>
        </form>
        </div>
        
    </div>
</div>




    
